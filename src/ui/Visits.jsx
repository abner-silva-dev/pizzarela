function Visits({ numVisits = 0 }) {
  return (
    <div>
      <p className="text-lg font-bold	">
        Numero de Visitas: <span>{numVisits}</span>
      </p>
    </div>
  );
}

export default Visits;
